import PhotoGallery from "@/components/gallery/Gallery";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <PhotoGallery />
    </div>
  );
};

export default page;
